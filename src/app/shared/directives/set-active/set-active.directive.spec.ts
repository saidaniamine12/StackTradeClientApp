import { SetActiveDirective } from './set-active.directive';

describe('SetActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SetActiveDirective();
    expect(directive).toBeTruthy();
  });
});
